version: 2

sphinx:
  configuration: docs/source/conf.py
  fail_on_warning: true

python:
   install:
   - requirements: docs/source/requirements.txt

python:
  install:
    - method: pip
      path: .
